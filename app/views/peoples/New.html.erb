# encoding: utf-8

<%= form_for @createdPeople, url: {action: "Create"} do |f| %>
    <%= f.text_field :name %>
    <%= f.text_field :room %>
    <%= f.text_field :internal_phone %>
    <%= f.submit "Добавить" %>
<% end %>